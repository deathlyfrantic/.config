if [[ $1 == "" ]];
then
    _this=$(basename "$0")
    echo "Usage: $_this input.flac [output.mp3]"
    exit 1
fi

if [[ $2 != "" ]];
then
    _outfile="$2"
else
    _outfile="$(basename "$1" ".flac").mp3"
fi

ffmpeg -v 5 -y -i "$1" -acodec libmp3lame -ac 2 -ab 320k "$_outfile"
