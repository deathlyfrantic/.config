# sadly i have to convert .eml -> .mbox on the regular :(
if [[ $2 != "" ]];
then
    _outfile="$2"
else
    _outfile=$(echo "$1" | cut -d "." -f 1)
    _outfile="$_outfile.mbox"
fi
formail -b < "$1" > "$_outfile"
